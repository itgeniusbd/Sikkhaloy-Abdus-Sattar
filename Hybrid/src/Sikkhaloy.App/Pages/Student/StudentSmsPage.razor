@page "/student/sms"
@layout StudentLayout
@inherits LocaleAwareComponent
@inject AppSession Session
@inject ISyncApiClient Api
@inject NavigationManager Nav

<div class="sp-page">
    <h1>@Locale.T("stu.sms")</h1>
    @if (loading)
    {
        <p class="sp-empty">@Locale.T("stu.loading")</p>
    }
    else if (rows.Count == 0)
    {
        <article class="sp-card"><p class="sp-empty">@Locale.T("stu.empty")</p></article>
    }
    else
    {
        @foreach (var row in rows)
        {
            <article class="sp-card" style="margin-bottom:0.7rem">
                <div class="sp-notice">
                    <b>@row.Purpose</b>
                    <small>@row.Phone • @(row.Date?.ToString("d MMM yyyy h:mm tt") ?? "")</small>
                    <p>@row.Text</p>
                </div>
            </article>
        }
    }
</div>

@code {
    private IReadOnlyList<StudentPortalSmsDto> rows = [];
    private bool loading = true;

    protected override async Task OnInitializedAsync()
    {
        if (!Session.IsStudent) { Nav.NavigateTo(Session.HomePath, replace: true); return; }
        try
        {
            if (!string.IsNullOrWhiteSpace(Session.AccessToken))
                rows = await Api.GetStudentPortalSmsAsync(Session.AccessToken);
        }
        catch { }
        loading = false;
    }
}
